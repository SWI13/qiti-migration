# مفاتيح Twilio — ما تدخلش هذا الملف لـ git أبداً
.env
.env.*
!.env.example

# Netlify
.netlify/
node_modules/

# Claude Code — نحبسو worktrees تاع الوكلاء، بصح تعريفاتهم تتبع في git
# باش تبقى نفس القواعد لكل واحد يخدم على المشروع
.claude/*
!.claude/agents/
