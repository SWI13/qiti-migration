/*
 * تسعيرة التوصيل حسب الولاية — شركة التوصيل DHD.
 *
 * ── علاش ملف وحدو ──────────────────────────────────────────────────
 * قبل، التوصيل كان رقم واحد لكل الجزائر: `SHIPPING = { home: 600,
 * desk: 400 }` في message.mjs. هذا يخدم غير كي تكون تبيع في ولاية ولا
 * زوج — كي توصّل لـ 58 ولاية، تمنراست ما تسواش قد بومرداس، وأي رقم
 * واحد يخسّرك في البعيد ولا يهرّب الزبون في القريب.
 *
 * ── القاعدة ─────────────────────────────────────────────────────────
 * كل رقم يتخلّص يجي من هنا. الصفحة تعرضو، السيرفر يعاود يحسبو، والطلب
 * يخزّن `shippingFee` وقت ما يتسجّل — فتبديل التسعيرة غداً ما يمسّش
 * الطلبيات القدام (شوف shippingFeeOf في message.mjs).
 *
 * ── شكل الجدول ──────────────────────────────────────────────────────
 * مفتاح = رقم الولاية الرسمي (1-58، شوف wilayas.mjs)، ماشي الاسم —
 * الاسم يتكتب بأكثر من صيغة ("الجزائر" / "الجزائر العاصمة") وأي فرق
 * حرف واحد يطيّح السطر على التسعيرة التلقائية بلا ما تنتبه.
 *
 *   { home: 800, desk: 450 }   توصيل للدار + مكتب
 *   { home: 1400, desk: null } الولاية بلا مكتب DHD — الزبون ما
 *                              يقدرش يختار "مكتب"، والفورم يعمي الخيار
 *   { home: null, desk: null } DHD ما توصّلش لهذي الولاية أصلاً —
 *                              الولاية تتعمى في الفورم والسيرفر يرفض
 *
 * الولاية اللي ما هيش في الجدول تاخذ DEFAULT_RATE. هذا مقصود: تسعيرة
 * ناقصة تبيع بالسومة العادية، ما تكسرش الطلب.
 */
import { WILAYAS, wilayaId } from './wilayas.mjs';

/* التسعيرة اللي كانت وحدة للكل — بقات كـ fallback للولايات اللي مازال
   ما وصلاتناش تسعيرتها من DHD */
export const DEFAULT_RATE = { home: 600, desk: 400 };

/*
 * الأرقام تاع DHD، 2026-08-18. الولاية اللي ما هيش في قائمة
 * DHD مكتوبة { home: null } — ماشي محذوفة: التصريح بيها هو اللي يخلّي
 * الفورم يعميها والسيرفر يرفضها، بدل ما تطيح على DEFAULT_RATE وتتباع
 * بسومة سهل ما تغطّيش التوصيل.
 */
export const RATES = {
   1: { home: 1100, desk:  600 },   // أدرار
   2: { home:  800, desk:  400 },   // الشلف
   3: { home:  900, desk:  500 },   // الأغواط
   4: { home:  700, desk:  400 },   // أم البواقي
   5: { home:  450, desk:  400 },   // باتنة
   6: { home:  700, desk:  400 },   // بجاية
   7: { home:  650, desk:  500 },   // بسكرة
   8: { home: 1100, desk:  600 },   // بشار
   9: { home:  500, desk:  400 },   // البليدة
  10: { home:  700, desk:  400 },   // البويرة
  11: { home: 1300, desk:  800 },   // تمنراست
  12: { home:  700, desk:  500 },   // تبسة
  13: { home:  800, desk:  400 },   // تلمسان
  14: { home:  800, desk:  400 },   // تيارت
  15: { home:  700, desk:  400 },   // تيزي وزو
  16: { home:  500, desk:  350 },   // الجزائر
  17: { home:  900, desk:  500 },   // الجلفة
  18: { home:  700, desk:  400 },   // جيجل
  19: { home:  600, desk:  400 },   // سطيف
  20: { home:  800, desk:  400 },   // سعيدة
  21: { home:  700, desk:  400 },   // سكيكدة
  22: { home:  800, desk:  400 },   // سيدي بلعباس
  23: { home:  700, desk:  400 },   // عنابة
  24: { home:  800, desk:  400 },   // قالمة
  25: { home:  600, desk:  400 },   // قسنطينة
  26: { home:  700, desk:  400 },   // المدية
  27: { home:  700, desk:  400 },   // مستغانم
  28: { home:  650, desk:  400 },   // المسيلة
  29: { home:  700, desk:  400 },   // معسكر
  30: { home:  900, desk:  500 },   // ورقلة
  31: { home:  700, desk:  400 },   // وهران
  32: { home: 1000, desk:  500 },   // البيض
  33: { home: 1300, desk:  600 },   // إليزي
  34: { home:  600, desk:  400 },   // برج بوعريريج
  35: { home:  700, desk:  400 },   // بومرداس
  36: { home:  800, desk:  400 },   // الطارف
  37: { home: 1300, desk:  600 },   // تندوف
  38: { home:  800, desk:  400 },   // تيسمسيلت
  39: { home:  800, desk:  500 },   // الوادي
  40: { home:  600, desk:  500 },   // خنشلة
  41: { home:  700, desk:  500 },   // سوق أهراس
  42: { home:  700, desk:  400 },   // تيبازة
  43: { home:  600, desk:  400 },   // ميلة
  44: { home:  700, desk:  400 },   // عين الدفلى
  45: { home: 1000, desk:  500 },   // النعامة
  46: { home:  700, desk:  400 },   // عين تموشنت
  47: { home:  900, desk:  500 },   // غرداية
  48: { home:  700, desk:  400 },   // غليزان
  49: { home: 1300, desk:  600 },   // تيميمون
  50: { home: null, desk: null },   // برج باجي مختار — DHD ما توصّلش لهنا
  51: { home:  900, desk:  500 },   // أولاد جلال
  52: { home: 1100, desk:  null },   // بني عباس — بلا مكتب
  53: { home: 1300, desk:  600 },   // عين صالح
  54: { home: null, desk: null },   // عين قزام — DHD ما توصّلش لهنا
  55: { home:  900, desk:  500 },   // تقرت
  56: { home: null, desk: null },   // جانت — DHD ما توصّلش لهنا
  57: { home:  900, desk:  null },   // المغير — بلا مكتب
  58: { home: 1000, desk:  500 },   // المنيعة
};

/** رقم الولاية من اسمها ولا من رقمها — يرجع null إذا ما تعرفش */
function idOf(wilaya) {
  if (typeof wilaya === 'number') return Number.isInteger(wilaya) && wilaya >= 1 && wilaya <= WILAYAS.length ? wilaya : null;
  return wilayaId(wilaya);
}

/** تسعيرة ولاية كاملة { home, desk } — DEFAULT_RATE إذا ما كانتش مسجّلة */
export function rateFor(wilaya) {
  const id = idOf(wilaya);
  return (id && RATES[id]) || DEFAULT_RATE;
}

/*
 * واش DHD توصّل لهذي الولاية.
 *
 * ثلاث ولايات (برج باجي مختار، عين قزام، جانت) ما هوماش في قائمة DHD.
 * مكتوبين { home: null } بدل ما نحيّدوهم من الجدول — لو حيّدناهم كانو
 * يطيحو على DEFAULT_RATE ويتباعو بـ 600 دج، وهي سومة ما تغطّي حتى نص
 * الطريق لجانت. التصريح بيهم يخلّي الفورم يعميهم والسيرفر يرفضهم.
 */
export const isServed = (wilaya) => rateFor(wilaya).home != null;

/** واش كاين مكتب DHD في هذي الولاية */
export const deskAvailable = (wilaya) => rateFor(wilaya).desk != null;

/*
 * سومة التوصيل اللي تتخلّص. ولاية بلا مكتب + طلب "مكتب" = سومة الدار،
 * ماشي صفر ولا خطأ — الطلب يوصل للدار وهي الحقيقة اللي يلزم تتخلّص.
 */
export function shippingFee(wilaya, mode) {
  const rate = rateFor(wilaya);
  /* ولاية بلا خدمة: الطلب مرفوض من الأصل (isServed فوق). هنا نرجعو
     التسعيرة الافتراضية ماشي null باش الحسابات القديمة — طلب مخزّن
     بلا shippingFee — ما تولّيش NaN في تقرير الشهر. */
  if (rate.home == null) return mode === 'desk' ? DEFAULT_RATE.desk : DEFAULT_RATE.home;
  return mode === 'desk' ? (rate.desk ?? rate.home) : rate.home;
}

/*
 * الجدول كامل للمتصفّح والعرض — [{ id, name, home, desk }] بترتيب الرقم.
 * نبنيوه من WILAYAS باش الصفحة ديما تعرض 58 ولاية حتى لو الجدول ناقص.
 */
export const rateTable = () => WILAYAS.map((name, index) => {
  const id = index + 1;
  const rate = rateFor(id);
  /* home: null يمشي كيما هو للمتصفّح — هو الإشارة اللي بيها يعمي
     الولاية في القائمة ويكتب "ما نوصلوش" في الجدول */
  return { id, name, home: rate.home ?? null, desk: rate.desk ?? null };
});

/*
 * الحمولة اللي تمشي للصفحة — الجدول الخام برك (58 سطر صغار، أقل من
 * 2 كيلو). المتصفّح يحسب بيه العرض، والسيرفر يعاود يحسب بنفس الملف.
 */
export const ratesPayload = () => ({ default: DEFAULT_RATE, table: rateTable() });

/*
 * وسم <script> بالجدول، باش الصفحة تقراه.
 *
 * ⚠️ نفس الوسم بالضبط في الزوج تاع الطرق: الصفحات المعروضة من الخادم
 * (render.mjs) تكتبو مع باقي الصفحة، والصفحة الستاتيك (index.html)
 * تاخذو محقون في scripts/build.mjs. طريق واحد في main.js — بلا نسخة
 * ثانية من 58 ولاية في المتصفّح.
 *
 * JSON في وسم مغلق ماشي متغيّر عام: المتصفّح ما ينفّذوش ككود، ونهربو
 * `<` باش نص فيه "</script>" ما يخرجش من الوسم.
 */
export const RATES_SCRIPT_ID = 'qiti-shipping-rates';

export const ratesScriptTag = () =>
  `<script type="application/json" id="${RATES_SCRIPT_ID}">${
    JSON.stringify(ratesPayload()).replace(/</g, '\u003c')
  }</script>`;
