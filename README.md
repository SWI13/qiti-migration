# Qiti — طوق ذكي بـ GPS للقطط

صفحة هبوط جاهزة للإنتاج لمنتج **Qiti**، طوق ذكي بـ GPS للقطط. كل المحتوى بالدارجة
الجزائرية، RTL كامل، وفيها فورم طلب (كاش عند الاستلام) على طريقة مواقع
التجارة الإلكترونية الجزائرية.

الصفحة ستاتيك بالكامل: **بلا build، بلا framework، بلا أي dependency وقت التشغيل.**
معاها فنكشنات صغيرة (Vercel Functions في `api/`) تبعثلك **إشعار تيليغرام على كل طلب**.

---

## كيفاش تشغّلها

```bash
# الصفحة + الفنكشنات + اللوحة (نفس حالة الإنتاج)
npx vercel dev

# الملفات برك (أسرع للوحة — بلا فنكشنات، بلا حساب)
PORT=8888 node scripts/dev-server.mjs

# فحوصات (118 فحص — أقسام، مصادقة، ميديا، حقول اللوحة)
npm run verify

# يبني dist/ — الملفات العامّة برك
npm run build
```

## النشر (Deploy)

الموقع يحتاج **Vercel** على خاطر فيه فنكشنات (`api/`). GitHub Pages ما يخدمش —
يخدم غير ملفات ستاتيك.

```bash
npx vercel --prod
```

ولا اربط الـ repo تاعك (`SWI13/qiti`) من لوحة Vercel، `vercel.json` يهدر على كلش:
أمر البناء، مجلّد النشر، الروابط (rewrites)، والتقارير المجدولة (crons).

---

## البنية

```
qiti/
├── index.html                    # الصفحة القديمة تاع الطوق (مازال هي اللي تخدم)
├── admin/                        # لوحة الإدارة — حملات، منتجات، لوحة القيادة، ميديا، معاينة
│   ├── index.html                # قشرة فارغة — admin/js/app.js يبني كلش
│   ├── css/                      # ستايل اللوحة (كلش var(--…) من styles.css)
│   └── js/                       # راوتينغ بالـ hash، صفحة لكل view، الفورم يتولّد من section-fields.js
├── assets/
│   ├── css/styles.css            # نظام التصميم كامل — كلش يمرّ على tokens
│   └── js/main.js                # ثيم، نافبار، أنيميشن، فورم الطلب، الإسناد
├── api/                          # Vercel Functions — الملف هو الرابط (/api/<اسم>)
│   ├── order.mjs                 # يستقبل الطلب، يسجّلو، ويبعث إشعار تيليغرام
│   ├── lead.mjs                  # يلتقط الطلب اللي ما كملش (رقم صحيح = lead)
│   ├── telegram-webhook.mjs      # نقرات قبول / رفض + المخزون + التكاليف
│   ├── render.mjs                # يعرض الحملات والمنتجات والفئات من المعطيات
│   ├── admin-api.mjs             # واجهة اللوحة (محميّة بالمصادقة)
│   ├── admin-login.mjs           # كلمة سرّ + كود تيليغرام
│   ├── media-upload.mjs          # رفع الصور (يتفحّص magic bytes)
│   ├── media-serve.mjs           # يخدم الصور المخزّنة
│   ├── track.mjs                 # بيكون الزيارات
│   ├── daily-report.mjs          # تقرير آخر النهار (00:00)
│   └── weekly-report.mjs         # تقرير الأسبوع (كل إثنين 00:00)
├── lib/                          # كود مشترك — ما يتنشرش للزائر
│   ├── blobs.mjs                 # طبقة التخزين — Redis + Vercel Blob وراء getStore()
│   ├── site.mjs                  # رابط الموقع الرسمي (ماشي من الطلب — أمان)
│   ├── cron-auth.mjs             # من يقدر يشغّل تقرير مجدول
│   ├── catalog.mjs               # المنتجات، الحملات، الفئات، المخزون، الروابط
│   ├── theme.mjs                 # ثيم الحملة → tokens (مقيّد، بلا CSS حر)
│   ├── media.mjs                 # تخزين الصور
│   ├── auth.mjs                  # جلسة الإدارة (كوكي ممضي)
│   ├── store.mjs                 # الطلبات + المخزون القديم + قائمة الحظر
│   ├── leads.mjs                 # الطلبات اللي ما كملوش + الإشعار المتأخّر
│   ├── visits.mjs                # عدّادات الزيارات (HINCRBY ذرّي)
│   ├── message.mjs               # رسالة تيليغرام، الأزرار، حساب الربح
│   ├── render/
│   │   ├── index.mjs             # سجلّ الأقسام + عقد القسم
│   │   ├── layout.mjs            # غلاف الصفحة (head, nav, footer, SEO)
│   │   ├── html.mjs              # الهروب — esc / escAttr / safeUrl
│   │   ├── sprite.mjs            # أيقونات SVG (مستخرجة من index.html)
│   │   └── sections/             # 10 أقسام: hero, order, gallery, faq…
│   ├── attribution.mjs           # منين جا الزبون (utm / fbclid / ttclid)
│   ├── meta.mjs                  # Meta CAPI — Lead عند الطلب، Purchase عند التوصيل
│   ├── analytics.mjs             # تجميع أرقام لوحة القيادة
│   ├── trust.mjs                 # فحص الثقة البرّاني (tkawen)
│   ├── wilayas.mjs               # الـ58 ولاية بترتيبها الرسمي (اسم → رقم)
│   └── shipping-rates.mjs        # سومة التوصيل لكل ولاية (DHD) — مصدر وحيد
├── scripts/
│   ├── build.mjs                 # ينسخ الملفات العامّة لـ dist/ (شوف تحت)
│   ├── check-admin-fields.mjs    # يتأكّد اللوحة تغطّي كل حقل قسم يقراه
│   ├── inject-rates.mjs          # يحقن جدول التوصيل في index.html الستاتيك
│   └── verify/                   # فحوصات — npm run verify
├── vercel.json                   # بناء + rewrites + crons (الترتيب مهم!)
├── package.json                  # scripts + @upstash/redis + @vercel/blob
└── README.md
```

### ⚠️ علاش كاين `dist/` وسكريبت بناء

كان مجلّد النشر هو جذر المستودع كامل، و`/lib/catalog.mjs` و`/lib/auth.mjs`
كانو يتقراو من برّا. دروك `npm run build` ينسخ غير `index.html` و`assets/`
و`admin/` لـ`dist/`، وهو اللي يتنشر (`outputDirectory` في `vercel.json`).
الفنكشنات تتبني وحدها من `api/` وما تمرّش على `dist/` أصلاً.

القائمة بيضا في [`scripts/build.mjs`](scripts/build.mjs) — **ملف جديد للزائر
لازم تزيدو ثمّة بيدك**، وإلا ما يتنشرش.

### اللوحة الإدارية — `/admin`

كلمة سر + كود يوصل في تيليغرام (نفس البوت تاع الطلبات)، ومن بعد كوكي ممضية
7 أيام. لازم تحطّ في Vercel: `ADMIN_PASSWORD_HASH` و`ADMIN_SESSION_SECRET`.

اللوحة تدير: حملات (إنشاء، نسخ، نشر، حذف)، منتجات (سومة، تكلفة، خيارات،
مخزون لكل فاريانت)، فئات، وصور. المعاينة المباشرة تنادي **نفس** `renderSections`
/`renderPage` اللي يستعملهم `render.mjs` — معاينة بكود آخر تكذب عليك يوم من
الأيام.

الفورم ما هوش مكتوب حقل بحقل: كل قسم عندو وصفة في `SECTION_FIELDS`
(في `admin/admin.js`)، واللوحة تبني منها. زيد حقل في قسم؟ زيدو في الوصفة —
و`npm run verify` يطيح إذا نسيت.

### كيفاش تتبنى صفحة حملة

```
/toji-outfit
     ↓  route:<path> في Blobs
  campaign  ──→  product (السومة، الخيارات، المخزون)
     ↓
  theme  →  ~15 token في <style>
     ↓
  sections[]  →  سجلّ الأقسام  →  HTML
     ↓
  layout  →  الصفحة كاملة
```

نفس الكود يعرض الطوق و Toji Outfit. اللي يتبدّل هو المعطيات برك — ماشي
ملف صفحة جديد لكل منتج.

---

## الأقسام

| # | القسم | الغرض منّو |
|---|-------|-----------|
| 1 | Hero | عنوان، سعر، زوج أزرار، كارت التتبّع المباشر + البطارية |
| 2 | Trust | تقييم، 5 badges، عدّادات أرقام متحرّكة |
| 3 | Features | 6 كارت بأيقونات |
| 4 | How it Works | 3 خطوات على خط رابط |
| 5 | Lifestyle | قسم عاطفي بصورة كبيرة |
| 6 | Product Gallery | معرض صور (thumbnails) + رسمة SVG للجهاز + مواصفات |
| 7 | Testimonials | 4 آراء زبائن بصورة، اسم، ولاية، تقييم |
| 8 | **Order Form** | فورم طلب كامل: بيانات، ولاية/بلدية، نوع التوصيل، كمية، ملخّص السعر مباشر |
| 9 | FAQ | 6 أسئلة (native `<details>`) |
| 10 | Final CTA | قسم إغلاق البيع |
| 11 | Footer | روابط + شبكات اجتماعية |

---

## إشعار تيليغرام على كل طلب

كي زبون يعمّر الفورم ويضغط "أكّد الطلب":

```
                         ┌→ 📱 تيليغرام تاعك — تفاصيل الطلب كاملة
الفورم → /functions/order ┤
                         └→ 💬 SMS للزبون — "طلبك تسجّل" (اختياري)
```

الفنكشن هي اللي تحوز المفاتيح، **ماشي الصفحة**. هذا مهم: الصفحة يشوفها كل
الناس، فلو حطّينا التوكن فيها أي واحد يقدر يسرقو.

### علاش تيليغرام وماشي واتساب؟

جرّبنا واتساب عبر Twilio بلا فايدة. المشكل ماشي في Twilio، المشكل في سياسة
واتساب روحها:

- ما تقدرش تبعث **نص حر** إلا داخل 24 ساعة من آخر رسالة بعثهالك المستقبل.
  برّا هذيك النافذة → خطأ `21654 ContentSid Required`.
- باش تبعث في أي وقت، لازم **template معتمد من Meta**. وكل مرّة تبدّل صيغة
  الرسالة، لازم تعاود تطلب الموافقة.
- حساب Twilio تاع Trial يبعث غير للأرقام المتحقّق منها.

| | واتساب (Twilio) | واتساب (Meta) | **تيليغرام** |
|---|---|---|---|
| الثمن | مدفوع | شبه مجاني | **مجاني بالكامل** |
| موافقة Meta | لازم | لازم | **لا** |
| نص حر في أي وقت | لا | لا | **نعم** |
| وقت الإعداد | ساعات | ساعات | **5 دقايق** |

إذا لازمك واتساب بالضبط (مثلاً الزبائن يعرفوك بيه)، الحل الرسمي هو **Meta
WhatsApp Cloud API** مع template فيه 6 متغيّرات. قوللي ونبدّلوها — الكود
معزول في دالة `notifyOwner()` وحدها.

> ⚠️ **ابعد على** Green API، Whapi، `whatsapp-web.js`، Baileys وأمثالهم.
> يخدمو بربط حساب واتساب تاعك الشخصي، وهذا يخالف شروط واتساب — الحسابات
> تتبلوكي. ما تديرهاش برقم الخدمة تاعك.

### الإعداد — 5 دقايق

**1. دير البوت** — حلّ تيليغرام، لقى [@BotFather](https://t.me/BotFather)،
ابعثلو `/newbot`، اختر اسم. يعطيك توكن شكلو:

```
1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
```

**2. جيب الـ chat id تاعك** — ابعث [@userinfobot](https://t.me/userinfobot)
أي رسالة، يجاوبك بالـ id تاعك (رقم مثل `987654321`).

**3. ابعث رسالة للبوت تاعك** — مهم! تيليغرام ما يخلّي البوت يبعثلك حتى
تبدا انت المحادثة. حلّ البوت واضغط **Start**.

**4. حطّ الـ environment variables في Vercel** (*Settings → Environment Variables*):

| المتغيّر | الشرح | مثال |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | من @BotFather — **سرّي** | `1234567890:AAHdq…` |
| `TELEGRAM_CHAT_ID` | id تاع الشات ولا الگروب | `-5105623360` |
| `TELEGRAM_WEBHOOK_SECRET` | كلمة سرّ تخترعها انت (للأزرار) | `أي نص عشوائي` |
| `TWILIO_ACCOUNT_SID` | *(اختياري)* لـ SMS الزبون | `ACxxxxxxxx…` |
| `TWILIO_AUTH_TOKEN` | *(اختياري)* — **سرّي** | `xxxxxxxx…` |
| `TWILIO_SMS_FROM` | *(اختياري)* رقم Twilio؛ بلاه ما يتبعثش SMS | `+17372212163` |
| `META_PIXEL_ID` | *(اختياري)* id تاع الـ pixel — بلاه ما يتبعث والو لميتا | `1234567890123456` |
| `META_CAPI_TOKEN` | *(اختياري)* access token — **سرّي** | `EAAG...` |
| `META_TEST_EVENT_CODE` | *(اختياري)* للتجريب برك — حيّدو من بعد | `TEST12345` |
| `TKAWEN_API_KEY` | *(اختياري)* مفتاح فحص الثقة — **سرّي** | `tkw_live_…` |
| `TKAWEN_API_URL` | *(اختياري)* رابط آخر (sandbox مثلاً) | `https://trust.tkawen.com/v1/check` |
| `ADMIN_PASSWORD_HASH` | للوحة الإدارة — sha256 تاع كلمة السرّ، ماشي الكلمة روحها | `9f86d081…` |
| `ADMIN_SESSION_SECRET` | يمضي كوكي الجلسة — **سرّي**، أي نص عشوائي طويل | `أي نص عشوائي` |
| `CRON_SECRET` | Vercel يبعثو في نداء التقارير المجدولة — بلاه التقرير يترفض | `أي نص عشوائي طويل` |
| `SITE_URL` | *(اختياري)* الدومين الرسمي؛ بلاه ياخذ دومين Vercel | `https://qiti.com` |
| `UPSTASH_REDIS_REST_URL` | يتحطّ **وحدو** كي تربط Upstash من لوحة Vercel | — |
| `UPSTASH_REDIS_REST_TOKEN` | نفس الشي — **سرّي** | — |
| `BLOB_READ_WRITE_TOKEN` | نفس الشي، كي تربط Vercel Blob — **سرّي** | — |

> 🔐 اللوحة تخدم غير إذا الزوج متغيّرات تاع `ADMIN_*` محطوطين. بلاهم، الدخول
> مسدود تماماً (fail closed) — ماشي مفتوح. باش تولّد الهاش:
> `node -e "console.log(require('node:crypto').createHash('sha256').update('كلمتك').digest('hex'))"`

للتجريب المحلي، دير ملف `.env` (مضاف في `.gitignore`) بنفس المتغيّرات وشغّل
`npx vercel dev`. ولا `npx vercel env pull` يجيبهم من المشروع مباشرةً.

**5. جرّب** — عمّر الفورم وشوف تيليغرام. إذا ما وصلاتش، شوف اللوغ:
*Vercel → Logs → api/order*. كل طلب يتسجّل في اللوغ حتى لو الإشعار فشل، فما
تخسر حتى زبون.

> **إذا حبيت الإشعار يجي لأكثر من واحد** (شريك، عامل): دير گروب في تيليغرام،
> زيد البوت فيه، واستعمل id تاع الگروب (يبدا بـ `-`) في `TELEGRAM_CHAT_ID`.

### أزرار قبول / رفض الطلب

كل إشعار يجي معاه ثلاث أزرار:

```
🐱 طلب جديد — Qiti

سارة بلقاسم
📞 +213770112233          ← تنقر عليه وتعيّط مباشرة
📍 الجزائر / باب الوادي
🚚 للدار — الكمية ×1

المجموع: 4,500 دج — كاش عند الاستلام

┌─────────────────┬─────────────────┐
│  ✅ قبول الطلب   │  ❌ رفض الطلب   │
├─────────────────┴─────────────────┤
│      💬 راسل الزبون واتساب         │
└───────────────────────────────────┘
```

**كي تنقر قبول** — الرسالة روحها تتبدّل وتزيد سطر أخضر، وأزرار القرار
تختفي (زر واتساب يبقى):

```
✅ مقبول — Abdo · 08/08 01:26
```

**كي تنقر رفض** — البوت يسقسيك على السبب أوّلاً. يردّ على رسالة الطلب
بـ ForceReply (الكيبورد يتحلّ وحدو)، وكي تكتب السبب:

```
❌ مرفوض — Abdo · 08/08 01:31
💬 الزبون ما جاوبش على التيليفون
```

ورسالة السؤال وجوابك يتمسحو، باش الگروب يبقى نظيف — تبقى غير رسالة الطلب
بحالتها النهائية.

> الطلب ما يتقرّرش مرّتين: إذا واحد آخر نقر بعد ما تقرّر، يوصلو تنبيه
> "الطلب راهو مقبول من قبل — Abdo" وما يتبدّل والو.

هكذا الگروب روحو يولّي سجلّ الطلبات: تشوف واش تقبّل، واش تّرفض، علاش،
وشكون دار واش.

> **ملاحظة على "الأخضر"**: تيليغرام ما يخلّيكش تلوّن النص. اللون الوحيد
> اللي نقدرو نستعملوه هو الإيموجي — علاش ✅ (أخضر) و ❌ (أحمر).

**علاش ما كانش زر "اتصل"؟** تيليغرام يرفض روابط `tel:` في الأزرار
(`Wrong port number specified in the URL` — جرّبناها). عوّضناها بحاجة أحسن:
الرقم مكتوب نص عادي بصيغة `+213…`، وتيليغرام يتعرّف عليه أوتوماتيكياً ويديرو
قابل للنقر — تنقر عليه في التيليفون وتعيّط.

#### تشبيك الأزرار — حلّ رابط برك

الأزرار يحتاجو **webhook**: تيليغرام لازم يعرف وين يبعث النقرة. بعد كل
deploy جديد (ولا كي تبدّل `TELEGRAM_WEBHOOK_SECRET`)، حلّ هذا الرابط:

```
https://qitishop.netlify.app/api/telegram-webhook?setup=1
```

الفنكشن تسجّل روحها عند تيليغرام: هي روحها تعرف الـ secret (من الـ env)
وتعرف رابط الموقع، فما تحتاجش تكتب حتى حاجة سرّية بيدك. الجواب يبان هكذا:

```json
{"ok":true,"url":"https://qitishop.netlify.app/api/telegram-webhook","pending":0}
```

**علاش الرابط ثابت وماشي ماخوذ من الطلب**: لو بنيناه من الـ request، أي
واحد يبعث `Host: evil.com` يحوّل الويبهوك لسيرفر تاعو — وتيليغرام يبعثلو
الـ secret في الـ header. علاش ناخذوه من `siteUrl()` في `lib/site.mjs` (يقرا
`SITE_URL` ولا `VERCEL_PROJECT_PRODUCTION_URL`)، فحتى لو حلّ الرابط شكون ما
كان، ديما يتسجّل نفس الموقع.

الـ secret روحو يمنع النقرات المزوّرة: تيليغرام يزيدو في كل طلب، والفنكشن
ترفض أي حاجة بلاه.

باش تتأكّد وقت ما تحب:

```bash
curl "https://api.telegram.org/bot<TOKEN>/getWebhookInfo"
```

لازم تشوف الـ `url` تاعك و`pending_update_count: 0`. إذا لقيت
`last_error_message`، هذاك هو سبب المشكل.

> **الأزرار ما تخدمش في `vercel dev` المحلي** — تيليغرام يحتاج رابط عمومي
> باش يوصلك. جرّبهم بعد الـ deploy، ولا استعمل `npx ngrok http 8888` إذا
> حبيت تجرّب محلياً.


### تقرير آخر النهار (00:00)

كل ليلة على **00:00 بتوقيت الجزائر** يوصلك تقرير تلقائي في نفس الگروب:

```
📊 تقرير 2026-08-08

📥 الطلبات: 4
✅ مقبولة: 2 (3 طوق)
❌ مرفوضة: 1
⏳ ما زال بلا قرار: 1

💰 مداخيل الطلبات المقبولة: 12,700 دج

أسباب الرفض:
• سارة بلقاسم — الزبون ما جاوبش

طلبات تستنّى قرار:
• كريم — وهران — 4,500 دج
```

سطر "ما زال بلا قرار" يبان غير كي يكونو طلبات ما نقرتش عليهم — يفكّرك
بيهم قبل ما يفوت الوقت.

**التوقيت**: الـ cron يخدم بـ UTC، والجزائر UTC+1 بلا توقيت صيفي، علاش
الجدول مكتوب `0 23 * * *` (23:00 UTC = 00:00 عندنا) — دروك في `vercel.json`
تحت `crons`، ماشي في الفنكشن روحها. التقرير يغطّي النهار اللي **كمل**.

**تشغيل يدوي** (تبدّل مع الهجرة): على Netlify كان ممنوع بـ 403. على Vercel
الرابط عمومي عادي، فالفنكشن ولّات تتحقّق بيدها:

```bash
curl "https://<موقعك>/api/daily-report?key=$TELEGRAM_WEBHOOK_SECRET"
```

⚠️ بلا مفتاح صحيح ترجّع `403`. نداء الـ cron تاع Vercel يمرّ بـ
`Authorization: Bearer $CRON_SECRET` — لازم `CRON_SECRET` تكون محطوطة في
متغيّرات البيئة، وإلا التقرير المجدول روحو يترفض (فشل مغلق مقصود: تقرير
ناقص يتلاحظ، رابط مفتوح للعالم لا).

### تقرير الأسبوع (كل إثنين 00:00)

كل يوم إثنين على **00:00 بتوقيت الجزائر** يوصلك ملخّص آخر 7 أيام (الإثنين
للأحد اللي فات) في نفس الگروب — نظرة من فوق بعيد على أسبوع كامل بدل يوم
وحدو:

```
📈 تقرير الأسبوع (2026-08-03 → 2026-08-09)

📥 مجموع الطلبات: 23
✅ مقبولة: 15
❌ مرفوضة: 6
⏳ ما زال بلا قرار (أقدم من أسبوع): 2

📦 توصّلت: 12 (14 طوق)
↩️ رجعت: 2

💰 مداخيل فعلية (طلبات توصّلت): 54,600 دج
💵 متوسّط قيمة الطلبية الموصّلة: 4,550 دج
🏆 أكثر ولاية طلبات: الجزائر (7)

طلبات تستنّى قرار من زمان:
• كريم — وهران — 4,500 دج
• سارة بلقاسم — البليدة — 4,900 دج
```

المداخيل تتحسب بنفس منطق تقرير آخر النهار: غير الطلبات اللي **توصّلت**
فعلاً (`deliveryStatus === 'delivered'`)، ماشي كل اللي "مقبولة" (تقدر ترجع
مع المُوصّل). سطر "ما زال بلا قرار" هنا معناه طلبات نسيتها من فوق أسبوع —
إشارة أخطر من نفس السطر في التقرير اليومي.

`store.mjs` ما فيهاش استعلام مدى تواريخ (غير `listOrdersForDay` ليوم
وحدو)، فـ `weekly-report.mjs` يستدعيها 7 مرّات (مرّة لكل يوم من الأسبوع)
ويجمع النتائج.

**التوقيت**: `00:00` يوم الإثنين بتوقيت الجزائر = `23:00 UTC` يوم **الأحد**
اللي قبلو (UTC لسّا ما دخلش الإثنين). علاش الجدول مكتوب `0 23 * * 0` في
`weekly-report.mjs` — الـ `0` الأخير هو الأحد في جدول cron الكلاسيكي
(0=الأحد … 6=السبت)، وماشي `1` (يلي كان يبعث التقرير بيوم كامل متأخّر).

نفس منطق التقرير اليومي في التشغيل اليدوي:
`curl "https://<موقعك>/api/weekly-report?key=$TELEGRAM_WEBHOOK_SECRET"`.

### وين تتخزّن الطلبات

في **Upstash Redis** (عبر Vercel) — كل شي JSON صغير: طلبات، منتجات، حملات،
مخزون، تكاليف، زيارات، جلسات الدخول. الصور وحدهم في **Vercel Blob**
(`media-bin`) — ملفات كبيرة ما عندهاش بلاصة في Redis.

كلش يعدّي على `getStore(name)` في [`lib/blobs.mjs`](lib/blobs.mjs) — نفس
التوقيع تاع `@netlify/blobs` القديم، علاش 104 نداءات في 6 ملفات ما تبدّلوش
كي هاجرنا. تبديل المزوّد مرّة ثانية = تكتب هذاك الملف وحدو.

الصفحة روحها باقية ستاتيك بلا حتى dependency — الحزم يستعملوهم غير الفنكشنات.

كل طلب يتسجّل **قبل** ما يتبعث الإشعار، يعني حتى لو تيليغرام طاح، الطلب
يبقى محفوظ ويبان في تقرير آخر النهار.


### الرسالة اللي توصل للزبون

إذا فعّلتي `TWILIO_SMS_FROM`، الزبون يوصلو SMS:

```
شكراً أمين! طلبك من Qiti تسجّل، نتصلو بيك قريباً باش نأكّدوه.
```


### السومة — منين تجي

الفنكشن **تحسب المجموع بروحها** وما تثقش في اللي يجي من المتصفّح (باش حتى واحد
ما يبدّل السعر بـ DevTools ويبعثلك طلب بـ 1 دج).

كاين زوج طرقات، حسب الصفحة:

| الصفحة | السومة تجي من | الحساب |
|---|---|---|
| صفحة حملة (`/toji-outfit`) | سجلّ المنتج في Blobs | `totalWith(unitPrice, …)` |
| `index.html` (الصفحة القديمة) | ثوابت `PRODUCT_PRICE` | `totalFor(…)` |

في صفحات الحملات، السومة تتبعث للمتصفّح في `<script id="qiti-pricing">` اللي
يكتبو السيرفر، والسيرفر يعاود يحسبها من عندو كي يوصل الطلب. بلاصة وحدة، ما
كاينش تكرار.

الصفحة القديمة ما فيهاش هذا الوسم، فـ `main.js` يرجع للثوابت وكلش يخدم كيما
قبل. كي تنقل الطوق لحملة، الثوابت هذي ما يبقاش عندها معنى وتتحيّد.

### سومة التوصيل — حسب الولاية (`lib/shipping-rates.mjs`)

التوصيل ماشي رقم واحد للجزائر كاملة: تمنراست ما تسواش قد بومرداس. الجدول
كامل في **بلاصة وحدة**: `lib/shipping-rates.mjs`، بمفتاح = رقم الولاية
الرسمي (ماشي الاسم — صيغة وحدة تختلف = سومة غالطة تعدّي بلا ما تنتبه).

    RATES = {
      16: { home:  500, desk:  350 },   // الجزائر
      11: { home: 1300, desk:  800 },   // تمنراست
      52: { home: 1100, desk: null },   // بني عباس — بلا مكتب
      56: { home: null, desk: null },   // جانت — DHD ما توصّلش لهنا
    }

الجدول فيه **الـ58 كاملين**: 55 ولاية بتسعيرة DHD، و3 بلا خدمة.

- **`desk: null`** = ما كاينش مكتب DHD (بني عباس 52، المغير 57 — جاو
  بـ 0 في قائمة DHD): الفورم يعمي الخيار، و`api/order.mjs` يرجّع الطلب
  للدار حتى لو جا "مكتب" من صفحة قديمة.
- **`home: null`** = DHD ما توصّلش لهذي الولاية (برج باجي مختار 50، عين
  قزام 54، جانت 56): الولاية تبان معمية في القائمة مكتوب عليها "ما
  نوصلوش"، والسيرفر يرفض الطلب برسالة واضحة. مصرّح بيهم بدل ما نحيّدوهم
  من الجدول — لو حيّدناهم كانو يطيحو على `DEFAULT_RATE` (600 دج) ويتباعو
  بسومة ما تغطّي حتى نص الطريق لجانت.
- **ولاية ما هيش في الجدول أصلاً** تاخذ `DEFAULT_RATE` — ما يوقعش دروك
  (الـ58 كاملين مكتوبين)، بصح يخلّي جدول ناقص يبيع بدل ما يطيح.

نفس الملف يخدم الجهتين: الصفحة تاخذ الجدول في وسم JSON
(`#qiti-shipping-rates`) — معروض مع الصفحة في `render/sections/order.mjs`،
ومحقون في الستاتيك عبر `scripts/inject-rates.mjs` — والسيرفر يعاود يحسب
بيه في `api/order.mjs`. الطلب يخزّن `shippingFee` وقت ما يتسجّل، فتبديل
التسعيرة غداً ما يمسّش الطلبيات القدام.

> `SHIPPING` في `lib/message.mjs` بقات غير كـ fallback (= `DEFAULT_RATE`).

### التكلفة والربح الصافي — `/cost`

تقرير آخر النهار يحسب "الربح الصافي التقديري" باش تعرف شحال ربحتي فعلاً،
ماشي غير المداخيل. ثلاث تكاليف تدخل في الحساب، وتتبدّل من تيليغرام مباشرة
(بلا `deploy` جديد) بأمر `/cost` — محصور في الشات المسجّل في
`TELEGRAM_CHAT_ID` كيما `/stock`:

```
/cost                  → يعرض التكاليف الثلاثة الحاليّة
/cost product 1600     → سوما البضاعة (لكل طوق)
/cost ads 250          → تكلفة الإعلانات (لكل طلب)
/cost returns 800      → خسارة الرجعة (لكل طلب رجع)
```

القيم الافتراضية (`productCost: 1500، adsCost: 300، returnLoss: 700`) في
`lib/store.mjs` (`DEFAULT_COSTS`) **تخمين برك** — بدّلهم بـ `/cost`
لأرقامك الحقيقية أوّل حاجة، وإلا رقم "الربح الصافي" في التقرير يبقى غلط.

تكلفة التوصيل (`COURIER_COST` = 350 دج) وحدها ماشي جزء من `/cost` — باقية
ثابتة في `lib/message.mjs` (تعديلها يحتاج `deploy`). الحساب:
`الربح = المجموع − (سوما البضاعة × الكمية) − تكلفة التوصيل − تكلفة الإعلانات`
للطلبات اللي توصّلت، و`−خسارة الرجعة` للطلبات اللي رجعت.

### منين جا الزبون (attribution) + Meta CAPI

الصفحة تقرا `utm_source` / `utm_medium` / `utm_campaign` / `utm_content` /
`utm_term` و `fbclid` / `ttclid` من الرابط، تخزّنهم في `localStorage`
(صلاحية 30 يوم، آخر نقرة تغلب)، وتبعثهم مع الطلب. القناة تبان في رسالة
تيليغرام في سطر `📣`، وتتخزّن في الطلب (`channel`) باش التقارير تقدر
تجمّع حسب القناة.

> ⚠️ **هذي المعلومة ما تتعوّضش لور.** الطلب اللي يتسجّل بلاها، عمرك ما
> تعرف منين جا. لازم تكون خدّامة **قبل** أوّل دينار تصرفو في الإعلانات.

في رابط الإعلان اكتب مثلاً:

```
https://qitishop.netlify.app/?utm_source=facebook&utm_medium=cpc&utm_campaign=collar-aout
```

`fbclid` و `ttclid` تزيدهم المنصّة روحها — ما تكتبهمش انت.

**Meta CAPI** (اختياري، يخدم غير إذا حطّيتي `META_PIXEL_ID` +
`META_CAPI_TOKEN`): الأحداث تتبعث **من السيرفر**، والمعلومات الشخصية
(الهاتف، الاسم، الولاية) تتهرّس بـ SHA-256 قبل ما تخرج — ميتا ما تشوف
عمرها القيم الأصلية.

| الحدث | وقتاش يتبعث | علاش |
|---|---|---|
| `Lead` | كي الزبون يعمّر الفورم | إشارة سريعة للخوارزمية |
| `Purchase` | كي تنقر **"📦 توصّل"** | الفلوس دخلت فعلاً — ماشي وقت القبول |

هذا هو الفرق المهم: في الدفع عند الاستلام، **الطلب المقبول ماشي فلوس**
(يقدر يرجع). لو بعثنا `Purchase` وقت القبول، ميتا تتعلّم تجيب ناس
يعمّرو الفورم ويرفضو عند الباب. نبعثوها غير على اللي خلّص.

### فحص الثقة (tkawen) + قائمة الحظر

طبقتين مختلفتين، وحدة تكمّل الأخرى:

| | المصدر | يشوف واش |
|---|---|---|
| `customerHistory` | طلباتك انت | زبون نصب **عندك** من قبل |
| **فحص الثقة** | خدمة برّانية (tkawen) | زبون نصب عند **تجّار آخرين** |
| **قائمة الحظر** | انت بيدك (`/block`) | القرار النهائي تاعك |

**الفحص البرّاني** يخدم غير إذا حطّيتي `TKAWEN_API_KEY`. على كل طلب،
الفنكشن تبعث رقم الزبون (بصيغة `+213…`)، رقم الولاية، قيمة الطلب و IP،
وترجع نتيجة تبان في الرسالة:

```
✅ فحص الثقة: موثوق (0.18)          ← الزبون عادي، بلا تفاصيل زايدة
🚫 فحص الثقة: خطر عالي (0.82)       ← العوامل تبان غير هنا
   • phone_history +0.45
   • refusal_rate +0.30
```

قواعد التصميم (مقصودة):

- **ما يوقّفش الطلب أبداً.** timeout 4 ثواني، وإذا الخدمة طاحت الطلب
  يكمّل بلا فحص. الزبون مستنّى في الصفحة — الطلب أهم من الفحص.
- **تنبيه، ماشي منع تلقائي.** أي نظام تنقيط عندو أخطاء، والزبون المرفوض
  بالغلط يضيع **بالسكات** — ما تعرفش حتى بلّي خسرتيه.
- **النتيجة تتخزّن** في الطلب، باش من بعد تشوف واش النقاط كانت تتنبّأ
  فعلاً بالرجعات — بدل ما تثق فيها على عمياني.

> ⚠️ **خصوصية**: هذا يبعث رقم الزبون و IP تاعو لخدمة برّانية على كل طلب.
> لازم يكون مكتوب في سياسة الخصوصية تاع الموقع (قانون 18-05).

**قائمة الحظر اليدوية** تخدم ديما، حتى بلا اشتراك في الفحص:

```
/block 0661445566 نصب في جوان    → يزيدو للقائمة
/unblock 0661445566              → يحيّدو
/blocked                         → يعرض الكل
```

الأرقام تتوحّد أوتوماتيكياً: `+213661445566` و `213661445566` و
`06 61 44 55 66` كلهم نفس المفتاح، فما يصرالش يتحظر رقم وحدة بزوج صيغ.

> ⚠️ **قائمة الولايات موجودة مرّتين**: في `assets/js/main.js` (متغيّر
> `WILAYAS`، باش تعمّر الـ`<select>`) وفي `lib/wilayas.mjs`
> (باش السيرفر يحوّل الاسم لرقم للخدمة البرّانية). نفس الترتيب الرسمي —
> إذا بدّلتي وحدة بدّل الثانية.

### التأكيد بالتيليفون قبل القبول

كل طلب جديد يجي معاه زر **"📞 تأكدت بالتيليفون"** فوق أزرار القبول/الرفض.
كي تنقرو، يتسجّل شكون أكّد ووقتاش، والزر يختفي.

**القبول ما يتمنعش بلا تأكيد قصداً.** كل طلب مقبول يتسجّل فيه
`confirmedBeforeAccept` (صح/غلط)، باش من بعد تقارن نسبة الرجعات بين
الطلبات المؤكّدة واللي ماشي مؤكّدة — وتقرّر بأرقامك انت واش المكالمة
تستاهل الوقت، بدل ما تتبع نصيحة عامّة.

### تاريخ الزبون بوجهين

كل طلب جديد يتفحّص رقم الهاتف في الطلبات القديمة، والنتيجة تتخزّن في
الطلب روحو (`customerHistory`):

- `✅ زبون موثوق: N طلبية توصّلت من قبل` — أحسن طلب يقدر يجيك.
- `⚠️ زبون عندو تاريخ: N رفض، M رجعة` — وإذا عندو حتى توصيلات ناجحة،
  تبان معاهم باش ما تحكمش عليه بنص القصّة.

التخزين (ماشي الحساب كل مرّة) مقصود: هكذا من بعد تقدر تشوف واش التنبيه
كان صحيح فعلاً — الطلبات المعلّمة بالأحمر واش رجعت أكثر من غيرها؟

### حالة الطلبات دروك — `/state`

بديل حيّ لتقرير آخر النهار، بلا ما تستنّى 00:00: `/state` (ولا `/status`)
يعرض ثلاث قوائم — طلبات بلا قرار، طلبات مقبولة في الطريق، ورجعات لسّا ما
وصلاتش فيزيائياً للمحل — مع قداش عندهم من وقت، علامة ⚠️ فوق أي طلب قديم من
24 سا، ومجموع الفلوس اللي لسّا معلّقة (بلا قرار + في الطريق، يعني ماشي
ضايعة، غير لسّا ما تأكّدتش).

### ⚠️ مسح كل شيء — `/clear`

`/clear` يمسح **كل الطلبات** (التاريخ كامل، بلا استثناء) ويرجّع **المخزون
لصفر**. فعل بلا تراجع، علاش يطلب تأكيد بزوج أزرار (✅ ايه، امسح كلش /
❌ لا، تراجعت) قبل ما يمسح والو — نقرة `/clear` وحدها ما تديرش حتى حاجة.

استعمله غير لو حبّيت تبدا مرحلة جديدة (تجريب خلص، ولا بزنس جديد بنفس
الكود) — ماشي للتنظيف العادي.

### حماية من السبام

- **فخّ بوتات** (honeypot): حقل `website` مخبّي في الفورم. البوتات تعمّرو،
  البشر لا. إذا جا معمّر، الفنكشن ترجع نجاح بلا ما تبعث والو.
- **تحقّق من جديد في السيرفر**: الاسم، الهاتف (`0[5-7]XXXXXXXX`)، الولاية،
  البلدية، والكمية (بين 1 و 10) كلهم يتفحصو مرّة ثانية في الفنكشن.

**58 ولاية** مسجّلين في `main.js` (متغيّر `WILAYAS`) ويتعمّرو أوتوماتيكياً في
الـ `<select>`.

---

## التقاط الطلبات اللي ما كملوش (leads)

الطلب ما يوجد حتى ينقر الزبون على "أكّد الطلب". الواحد يكتب اسمو ورقمو، يتلهّى،
ويسكّر الصفحة — ومن ناحيتنا ما صرا والو. وهذاك واحد كان **راه يشري**، وعندنا
رقم تيليفونو.

دروك: ساعة ما الرقم يولّي صحيح، يتسجّل `lead`. إذا كمّل، يولّي طلب عادي. إذا لا،
يبقى في لائحة تعيّطلو منها.

### كيفاش تخدم

```
الزبون يكتب رقم صحيح
        │
        ├─ 900ms سكوت ──────────→  POST /api/lead   يتسجّل، تيليغرام ساكت 🔇
        ├─ يعمّر الولاية ────────→  POST /api/lead   يتسجّل، تيليغرام ساكت 🔇
        │
        │   ⏱️ سكت دقيقتين بلا ما يمسّ والو
        │   ولا خرج من الصفحة (سكّرها / بدّل تطبيق)
        │                    │
        │                    ▼
        │            📨 رسالة تيليغرام وحدة
        │       [💬 واتساب] [📞 عيّطتلو] [🗑️ شطبو]
        │                    │
        └─ رجع وعمّر حقل ────→ ✏️ نفس الرسالة تتبدّل
        └─ أكّد الطلب ───────→ ✅ نفس الرسالة تولّي "كمّل الطلب"
```

**علاش السكوت مهمّ:** الزبون العادي ياخذ دقيقتين-ثلاثة باش يعمّر — يقرا،
يسأل على العنوان، يعاود يشوف السومة. إشعار في ثانية 10 يوصلك على واحد راه
قدّامك غادي يأكّد، وتولّي ما تثقش في الإشعارات.

**شكون يقيس السكوت:** الصفحة روحها (`LEAD_IDLE_MS` في `main.js`). الفنكشنات
serverless ما تقدرش تستنّى، وكرون كل دقيقة ما يتقبلش في Hobby. السيرفر عندو
حاجز تاعو (`NOTIFY_AFTER_SECONDS`) باش ما يثقش في ساعة المتصفّح — فحص في
`verify-leads` يتأكّد بلي الزوج متوافقين.

**خرج من الصفحة = يتبعث دروك**، بلا انتظار. وإذا كان غير بدّل تطبيق ورجع
وكمّل، الرسالة روحها تتصحّح لـ "✅ كمّل الطلب" — إشعار جا بكري ما يكلّف والو.

### في اللوحة

الـ leads يبانو في **نفس صفحة الطلبات** ببادج `Lead` (إطار مقطّع — ماشي طلب
بعد)، مع فلتر خاص بيهم. النقر على الاسم يحلّ نافذة تقول صراحةً بلي السومة هي
اللي كانت في السلّة، ماشي فلوس تستنّى.

⚠️ ما يدخلوش في `state.orders`: كل حساب فلوس (المداخيل، لوحة القيادة، بادج
"بلا قرار") مبني عليها، وخلطهم فيها يخلّي الأرقام تعدّ ناس ما شراوش.

### القرارات اللي تستاهل تعرفهم

| القرار | علاش |
|---|---|
| المفتاح هو رقم التيليفون | نفس الزبون يحلّ الصفحة 3 مرّات = سجلّ واحد، ماشي 3 |
| TTL 30 يوم | مادّة استرجاع ماشي محاسبة — رقم عمرو شهرين ما ينفع، ويبقى معلومة شخصية بلا سبب |
| رسالة وحدة تتبدّل، ماشي وحدة جديدة كل مرّة | الزبون يعمّر 4 حقول = 4 رسائل على نفس الواحد. الگروب يتعمّر ويولّي ما يتقراش |
| الإشعار يستنّى دقيقتين سكوت (ولا خروج من الصفحة) | التسجيل لازم يكون فوري وإلا نخسرو الرقم، بصح الإشعار الفوري يجيك على ناس راهم يعمّرو عادي |
| الأرقام المحظورة ما توصلش | حظرتيهم لسبب — الكنس يشطبهم بلا إشعار |
| الجواب ديما `{ok:true}` | حتى للرقم المحظور. أي جواب مختلف يولّي المسار أداة تفحص بيها الأرقام من برّا |

### الأوامر

```
/leads      اللائحة كاملة — الاسم، الرقم، قداش عمّر، وقتاش آخر حركة
```

وتبان تاني في تقرير آخر النهار (أوّل 10، والباقي في `/leads`).

### ⚠️ الكنس — شبكة أمان برك

الإشعار العادي يتبعث فوراً من `api/lead.mjs`. `sweepLeads()` تخدم غير للـ leads
اللي فشل إشعارهم (تيليغرام كان طايح، الشبكة قطعت) — تعاود تجرّب.

ما عندهاش cron خاص: خطّة Vercel Hobby تسمح بـ cron مرّة في النهار برك. تركب
على الحركة اللي كاينة — كل lead جديد، كل طلب، وتقرير آخر النهار ينادوها.

إذا رقّيتي لـ Pro، زيد في `vercel.json`:

```json
{ "path": "/api/lead-sweep", "schedule": "0,15,30,45 * * * *" }
```

ونادي `sweepLeads()` من الفنكشن الجديد. الباقي يبقى كيما هو.

### ⚠️ عدد الفنكشنات

`api/` فيه دروك **11 فنكشن**. خطّة Hobby تسمح بـ 12. الفنكشن الجاي هو الأخير
قبل ما تلزمك الترقية ولا دمج فنكشنات مع بعضها.

---

## نظام التصميم

كل الـ tokens فوق في `styles.css` تحت `:root`. بدّلهم من هناك وكل الصفحة تتبع
— ما تحتاجش تدخل تبدّل كل rule بوحدها.

```css
--orange-500: #FF6B2C;   /* اللون الأساسي */
--cream:      #FDF8F3;   /* خلفية دافية */
--ink:        #14110F;   /* لون النص — أسود دافي، ماشي #000 خالص */
--r-lg: 24px;            /* سلّم الـ radius: sm/md/lg/xl/full */
--section-y: clamp(72px, 9vw, 132px);
```

**الخط** — `Tajawal` لكلش. مختار خصّيصاً على خاطر عندو وزن 900 حقيقي (أغلب
خطوط العربي ما عندهاش)، وهذا اللي يعطي العناوين الثقيلة شكلها.

**⚠️ مهم إذا بدّلتي لغة الصفحة**: الكتابة العربية سكريبت متّصل — استعمال
`letter-spacing` سلبي (تقنية شائعة في التصميم اللاتيني للعناوين الكبيرة) يهرّس
الحروف المتّصلة. كل الـ headings في هذا الملف مصفّاة من هذا، خلّيها هكاك إذا
زدتي عناوين جداد بالعربي.

**Dark mode** — مجموعة tokens ثانية كاملة تحت `[data-theme="dark"]`. الثيم
يتطبّق بسكريبت صغير في `<head>` *قبل ما ترسم الصفحة*، فما يبانش وميض. التفضيل
يتخزّن في `localStorage`، وإذا المستخدم ما اختارش، تتبع إعدادات النظام.

**RTL** — الصفحة `dir="rtl"` بالكامل. أغلب التخطيط (flex/grid) يتقلب أوتوماتيكياً
مع الاتجاه بلا أي كود إضافي. المواضع المطلقة (الكروت العائمة، الأيقونة داخل
الحقول) تستعمل `inset-inline-start`/`inset-inline-end` (منطقية) بدل
`left`/`right` (فيزيائية) باش تتقلب صحيح. السهم `#i-arrow-rtl` في الـ sprite
مرسوم يشير لليسار مباشرة (اتجاه "القدّام" في RTL) بدل ما نعتمدو على
`transform: scaleX(-1)`.

---

## بدّل المحتوى بمحتواك

**الصور** حالياً من Unsplash (شفتهم بعيني وحدة وحدة، كلهم قطط حقيقية). فيها
حاجة خاصّك تعرفها: **ما كايناش صور حقيقية لمنتج Qiti** (المنتج ماشي موجود
حقيقة) — الصور المستعملة هي قطط لابسين أطواق عادية (أقرب حاجة لقيتها). كي
يكون عندك تصوير حقيقي للمنتج، بدّل:
1. `src` في `#galleryImg` وصور hero.
2. الـ 6 `data-src` في أزرار `.thumb`.
3. رسمة الجهاز SVG (داخل `.showcase__stage`) — رسمة يدوية، ماشي صورة، فبدّلها
   بصورة حقيقية كي تكون عندك.

**السعر** (3900 دج) مكتوب مباشرة في `index.html` (4 بلايص)، `main.js`
(`PRODUCT_PRICE`)، و `api/order.mjs` (`PRODUCT_PRICE`).
بدّلهم كامل مع بعض — شوف تحذير "الأسعار مكتوبة في بلاصتين" فوق.

**عدد الزبائن** (200+) في stats، rating pill، وعنوان قسم التقييمات.

---

## ملاحظات الوصول (Accessibility) والأداء

- Skip link، عناصر `<button>`/`<a>` حقيقية، focus rings بايّنة.
- FAQ تستعمل `<details>`/`<summary>` — تخدم حتى بلا جافاسكريبت.
- بلا جافاسكريبت، الصفحة تبقى مقروءة كاملة؛ غير الأنيميشن، الثيم، وفورم الطلب
  اللي ما يخدموش (الفورم HTML يبقى بايّن بصح ما يقدرش يتحقّق أو يبعث).
- `prefers-reduced-motion` محترم بالكامل.
- `will-change` يتحطّ غير وقت الأنيميشن الفعلي، باش الصفحة ما تبقاش شادّة
  compositor layers بزاف بعد التحميل.
- صورة الـ hero بـ `fetchpriority="high"`؛ كل الصور تحت الطيّة `loading="lazy"`.
- الأيقونات كلهم SVG sprite واحد — بلا أي مكتبة أيقونات جافاسكريبت.

---

## بخصوص Tailwind

الطلب الأصلي ذكر Tailwind. هذا المشروع مبني بـ **CSS مكتوب يدوياً بنظام tokens**
عوض هذا، قصداً:

- Tailwind Play CDN يترجم في المتصفح وقت التشغيل — يزيد ~300KB جافاسكريبت
  ويعمل flash of unstyled content. ماشي معمول للإنتاج، وطلبتي صفحة سريعة بزاف.
- Tailwind build حقيقي يحتاج npm، config، وخطوة build لصفحة ستاتيك وحدة.

النتيجة: ملف CSS واحد ~35KB، بلا dependencies، بلا build. إذا تحب Tailwind +
Vite حقيقي، أسماء الـ tokens تتحول مباشرة لـ `theme.extend` — قوللي ونديرها.
